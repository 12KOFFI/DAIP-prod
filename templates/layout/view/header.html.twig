
<header class="text-white" style="background-color: #CC5500; margin-bottom: 8rem;">
    <nav class="navbar navbar-expand-lg navbar-dark bg-orange py-3">
        <div class="container">
            <div class="d-flex align-items-center justify-content-between w-100 gap-3">
                <a class="navbar-brand d-flex align-items-center gap-2" href="{{ path('app_home') }}">
                    <img src="{{ asset('front/image/logo.png') }}" alt="Logo" height="40" class="d-inline-block align-text-top">
                    <span class="fw-semibold text-uppercase letter-spacing-1 d-none d-lg-inline">Accueil</span>
                </a>
                <button class="navbar-toggler ms-3" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Basculer la navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
            </div>

            <div class="collapse navbar-collapse mt-3 mt-lg-0 w-100" id="navbarNav">
                <div class="d-lg-flex align-items-center justify-content-between w-100 gap-4">
                    <ul class="navbar-nav flex-column flex-lg-row align-items-center align-items-lg-start gap-3 gap-lg-4 mb-3 mb-lg-0 w-100">
                        <li class="nav-item w-100 w-lg-auto">
                            <a class="nav-link active px-3 py-2 fw-semibold d-flex align-items-center justify-content-center justify-content-lg-start gap-2 border border-light border-opacity-10 rounded w-100" href="{{ path('app_liste_projet') }}">
                                <i class="bi bi-grid-1x2-fill fs-5"></i>
                                <span>NOS OFFRES DISPONIBLES</span>
                            </a>
                        </li>
                    </ul>
                    <div class="d-flex flex-column flex-lg-row justify-content-center justify-content-lg-end gap-3 w-100 w-lg-auto">
                        {% if app.user %}
                            <div class="dropdown w-100 w-lg-auto">
                                <button class="btn btn-outline-light dropdown-toggle d-flex align-items-center justify-content-center gap-2 w-100" type="button" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                                    <i class="bi bi-person-badge fs-5"></i>
                                    <span class="text-truncate" style="max-width: 150px;">{{ app.user.email }}</span>
                                </button>
                                <ul class="dropdown-menu dropdown-menu-end w-100 w-lg-auto shadow">
                                    <li><a class="dropdown-item py-2" href="{{ path('app_profile') }}"><i class="bi bi-person me-2"></i>Mon profil</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item py-2 text-danger" href="{{ path('app_logout') }}"><i class="bi bi-box-arrow-right me-2"></i>Déconnexion</a></li>
                                </ul>
                            </div>
                        {% else %}
                            <a href="{{ path('app_login') }}" class="btn btn-outline-light d-inline-flex align-items-center justify-content-center py-2 px-4 gap-2 w-100 w-lg-auto">
                                <i class="bi bi-person-circle fs-5"></i>
                                <span>Mon espace</span>
                            </a>
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </nav>
</header>
