{{ form_start(form) }}
    {{ form_row(form.libelle) }}

    <div class="d-flex gap-2 mt-3">
        <button class="btn btn-primary" type="submit">{{ button_label|default('Enregistrer') }}</button>
        <a href="{{ path('app_type_evaluation_index') }}" class="btn btn-light">Annuler</a>
    </div>
{{ form_end(form) }}
