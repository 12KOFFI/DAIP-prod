{% extends 'base.html.twig' %}

{% block title %}Nouveau type d'évaluation{% endblock %}

{% block page_content %}
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h1 class="h4 mb-0">Nouveau type d'évaluation</h1>
        <a href="{{ path('app_type_evaluation_index') }}" class="btn btn-outline-secondary btn-sm">Retour</a>
    </div>

    <div class="card">
        <div class="card-body">
            {{ include('type_evaluation/_form.html.twig', {form: form, button_label: 'Créer'}) }}
        </div>
    </div>
{% endblock %}
